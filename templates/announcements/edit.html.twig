{% extends 'base.html.twig' %}

{% block title %}Editer une annonce{% endblock %}

{% block body %}
    <h1 class="text-bold text-white">Editer mon annonce</h1>

    {{ include('announcements/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    <div class="lien">
        <a href="{{ path('announcements_index') }}">Retour</a>
    </div>
    {{ include('announcements/_delete_form.html.twig') }}
{% endblock %}