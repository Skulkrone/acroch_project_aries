{% extends 'base.html.twig' %}

{% block title %}Annonces{% endblock %}

{% block body %}
    {% if is_granted('ROLE_ADMIN') %}
        <h1 class="text-bold text-white">Toutes les annonces</h1>
    {% elseif is_granted(['ROLE_USER','ROLE_ADD']) %}
        <h1 class="text-bold text-white">Mes annonces</h1>
    {% endif %}
    

    <!--<table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Description</th>
                <th>Price</th>
                <th>Size</th>
                <th>Weight</th>
                <th>Quality</th>
                <th>Specifications</th>
                <th>ImageAnnouncements</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>

    {% for announcement in announcements %}
        {% if announcement.fkUserId is same as(app.user) %}
            <tr>
                <td>{{ announcement.id }}</td>
                <td>{{ announcement.Description }}</td>
                <td>{{ announcement.Price }}</td>
                <td>{{ announcement.Size }}</td>
                <td>{{ announcement.Weight }}</td>
                <td>{{ announcement.Quality }}</td>
                <td>{{ announcement.Specifications }}</td>
                <td>{{ announcement.ImageAnnouncements }}</td>
                <td>
                    <a href="{{ path('announcements_show', {'id': announcement.id}) }}">show</a>
                    <a href="{{ path('announcements_edit', {'id': announcement.id}) }}">edit</a>
                </td>
            </tr>

        {% elseif is_granted('ROLE_ADMIN') %}
            <tr>
                <td>{{ announcement.id }}</td>
                <td>{{ announcement.Description }}</td>
                <td>{{ announcement.Price }}</td>
                <td>{{ announcement.Size }}</td>
                <td>{{ announcement.Weight }}</td>
                <td>{{ announcement.Quality }}</td>
                <td>{{ announcement.Specifications }}</td>
                <td>{{ announcement.ImageAnnouncements }}</td>
                <td>
                    <a href="{{ path('announcements_show', {'id': announcement.id}) }}">show</a>
                    <a href="{{ path('announcements_edit', {'id': announcement.id}) }}">edit</a>
                </td>
            </tr>
        {% elseif app.user == false %}
            <tr>
                <td>{{ announcement.id }}</td>
                <td>{{ announcement.Description }}</td>
                <td>{{ announcement.Price }}</td>
                <td>{{ announcement.Size }}</td>
                <td>{{ announcement.Weight }}</td>
                <td>{{ announcement.Quality }}</td>
                <td>{{ announcement.Specifications }}</td>
                <td>{{ announcement.ImageAnnouncements }}</td>
            </tr>
        {% endif %}


    {% endfor %}

</tbody>
</table>-->

    <!-- Page Content -->
    <div class="container">

        <div class="row">

            <!--<div class="col-lg-3">

                <h1 class="my-4 text-white">Shop Name</h1>
                <div class="list-group">
                    <a href="#" class="list-group-item">Category 1</a>
                    <a href="#" class="list-group-item">Category 2</a>
                    <a href="#" class="list-group-item">Category 3</a>
                </div>

            </div>-->
            <!-- /.col-lg-3 -->

            <div class="col-lg-9">

                <!--<div id="carouselExampleIndicators" class="carousel slide my-4" data-ride="carousel">
                    <ol class="carousel-indicators">
                        <li data-target="#carouselExampleIndicators" data-slide-to="0" class="active"></li>
                        <li data-target="#carouselExampleIndicators" data-slide-to="1"></li>
                        <li data-target="#carouselExampleIndicators" data-slide-to="2"></li>
                    </ol>
                    <div class="carousel-inner" role="listbox">
                        <div class="carousel-item active">
                            <img class="d-block img-fluid" src="/../../image/RB-Streamer-Standard-4.jpg" alt="First slide">
                        </div>
                        <div class="carousel-item">
                            <img class="d-block img-fluid" src="/../../image/warwick.jpg" alt="Second slide">
                        </div>
                        <div class="carousel-item">
                            <img class="d-block img-fluid" src="/../../image/RB-Streamer-Standard-4.jpg" alt="Third slide">
                        </div>
                    </div>
                    <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                        <span class="sr-only">Previous</span>
                    </a>
                    <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                        <span class="sr-only">Next</span>
                    </a>
                </div>-->

                <div class="row">
                    {% for announcement in announcements %}
                        {% if announcement.fkUserId is same as(app.user) %}
                            <div class="col-lg-4 col-md-6 mb-4">
                                <div class="card h-100">
                                    <a href="#"><img class="card-img-top" src="{{ announcement.ImageAnnouncements }}" alt=""></a>
                                    <div class="card-body">
                                        <h4 class="card-title">
                                            <a href="{{ path('announcements_show', {'id': announcement.id}) }}">{{ announcement.Description }}</a>
                                        </h4>
                                        <h5>{{ announcement.Price }} €</h5>
                                        <p class="card-text">{{ announcement.Specifications }}</p>
                                    </div>

                                </div>
                            </div>
                        {% elseif is_granted('ROLE_ADMIN') %}
                            <div class="col-lg-4 col-md-6 mb-4">
                                <div class="card h-100">
                                    <a href="#"><img class="card-img-top" src="{{ announcement.ImageAnnouncements }}" alt=""></a>
                                    <div class="card-body">
                                        <h4 class="card-title">
                                            <a href="{{ path('announcements_show', {'id': announcement.id}) }}">{{ announcement.Description }}</a>
                                        </h4>
                                        <h5>{{ announcement.Price }} €</h5>
                                        <p class="card-text">{{ announcement.Specifications }}</p>
                                    </div>

                                </div>
                            </div>
                        {% elseif app.user == false %}
                            <div class="col-lg-4 col-md-6 mb-4">
                                <div class="card h-100">
                                    <a href="#"><img class="card-img-top" src="{{ announcement.ImageAnnouncements }}" alt=""></a>
                                    <div class="card-body">
                                        <h4 class="card-title">
                                            <a href="{{ path('announcements_show', {'id': announcement.id}) }}">{{ announcement.Description }}</a>
                                        </h4>
                                        <h5>{{ announcement.Price }} €</h5>
                                        <p class="card-text">{{ announcement.Specifications }}</p>
                                    </div>

                                </div>
                            </div>
                        {% endif %}
                    {% endfor %}
                </div>
            </div>

        </div>
        <!-- /.row -->

    </div>
    <!-- /.col-lg-9 -->

</div>
<!-- /.row -->

</div>
<!-- /.container -->
{% if app.user == false %}
{% else %}
    <div class="lien">
        <a href="{{ path('announcements_new') }}">Ajouter une nouvelle annonce</a>
    </div>
{% endif %}

{% endblock %}