{% extends 'base.html.twig' %}

{% block title %}Nouvelle Annonce{% endblock %}

{% block body %}
    <h1 class="text-bold text-white">Créer et publier une annonce</h1>

    {{ include('announcements/_form.html.twig') }}
    <div class="lien">
        <a href="{{ path('announcements_index') }}">back to list</a>
    </div>
{% endblock %}