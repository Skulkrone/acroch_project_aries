{% extends 'base.html.twig' %}

{% block title %}Invoices{% endblock %}

{% block body %}
    <h1>Invoices</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ invoice.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ invoice.Date ? invoice.Date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Cost</th>
                <td>{{ invoice.Cost }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('invoices_index') }}">back to list</a>

    <a href="{{ path('invoices_edit', {'id': invoice.id}) }}">edit</a>

    {{ include('invoices/_delete_form.html.twig') }}
{% endblock %}