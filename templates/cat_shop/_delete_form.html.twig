<form method="post" action="{{ path('cat_shop_delete', {'id': cat_shop.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cat_shop.id) }}">
    <button class="btn">Delete</button>
</form>