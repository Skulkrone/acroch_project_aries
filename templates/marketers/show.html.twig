{% extends 'base.html.twig' %}

{% block title %}Marketers{% endblock %}

{% block body %}
    <h1>Marketers</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ marketer.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ marketer.Label }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ marketer.Description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('marketers_index') }}">back to list</a>

    <a href="{{ path('marketers_edit', {'id': marketer.id}) }}">edit</a>

    {{ include('marketers/_delete_form.html.twig') }}
{% endblock %}