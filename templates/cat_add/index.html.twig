{% extends 'base.html.twig' %}

{% block title %}CatAdd index{% endblock %}

{% block body %}
    <h1>CatAdd index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Label</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for cat_add in cat_adds %}
            <tr>
                <td>{{ cat_add.id }}</td>
                <td>{{ cat_add.Label }}</td>
                <td>
                    <a href="{{ path('cat_add_show', {'id': cat_add.id}) }}">show</a>
                    <a href="{{ path('cat_add_edit', {'id': cat_add.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('cat_add_new') }}">Create new</a>
{% endblock %}